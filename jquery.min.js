$(function() {
  otable = $('#dt').dataTable();
})

function filterme() {
  //build a regex filter string with an or(|) condition
  var types = $('input:checkbox[name="Year"]:checked').map(function() {
    return '^' + this.value + '\$';
  }).get().join('|');
  //filter in column 3, with an regex, no smart filtering, no inputbox,not case sensitive
  otable.fnFilter(Years, 2, true, false, false, false);


}
