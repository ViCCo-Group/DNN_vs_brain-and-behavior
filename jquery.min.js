document.getElementsByClass("year").addEventListener("click", filterTable); -> damit wird die funktion unten aufgerufen wenn ein keyword geklickt wird bzw mit den buttons wie in dem w3schools beispiel

function filterTable(event) {
    rows = document.getElementsByTag("tr") -> damit holt man sich alle Zeilen
   
   for(int i=0; i<rows.length; i++) {
       if() { -> hier muesste man checken ob das keyword drin vorkommt
            rows[i].css({'display': 'block'}) -> damit wird die Zeile sichtbar
       } else {
            rows[i].css({'display': 'none'}) -> damit wird die Zeile unsichtbar
       }
   }
}




jQuery(document).on('ready', function() {
    var filter_magic = function(e) {
        var trs = jQuery('.sortable tr:not(:first)');

        trs.hide();
        var showAll = true;
        jQuery('input[type="checkbox"][name="filter"]').each(function() {
            if (jQuery(this).is(':checked')) {
                var val = jQuery(this).val();
                trs.each(function() {
                    var tr = jQuery(this);
                    var td = tr.find('td:nth-child(3)');
                    if (td.text() === val) {
                        tr.show();
                        showAll = false;
                    }
                });
            }
        });
        if (showAll) {
            trs.show();
        }
    };

    jQuery('input[type="checkbox"][name="filter"]').on('change', filter_magic);
    filter_magic();
});
